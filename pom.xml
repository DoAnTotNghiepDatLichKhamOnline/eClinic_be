<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>iuh.fit.fe</groupId>
    <artifactId>be_webSatLichKham</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>be_webSatLichKham</name>
    <description>eClinic backend - kiến trúc service-based: nhiều service Spring Boot, dùng chung 1 MySQL</description>

    <!--
        common            : thư viện dùng chung (toàn bộ entity, enum, exception, Flyway migration)
        api-gateway       : 8080 - cổng vào duy nhất cho frontend (định tuyến + kiểm JWT)
        identity-service  : 8081 - AUTH-01..05, ADM-03
        catalog-service   : 8082 - ADM-01, ADM-02, AUTH-04, DOC-01..03
        scheduling-service: 8083 - SCHED-01..06
        booking-service   : 8084 - BOOK-01..10, PAT-01..03, ADM-04
        medical-service   : 8085 - EXAM-01..05, DOC-02
        notification-service: 8086 - NOTI-01 + gửi email
        chatbot-service   : 8087 - CHAT-01
        report-service    : 8088 - DASH-01..06, ADM-05
        (Mã YC theo file yeu-cau-toan-dien-dat-lich-kham-benh.md)
    -->
    <modules>
        <module>common</module>
        <module>api-gateway</module>
        <module>identity-service</module>
        <module>catalog-service</module>
        <module>scheduling-service</module>
        <module>booking-service</module>
        <module>medical-service</module>
        <module>notification-service</module>
        <module>chatbot-service</module>
        <module>report-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>iuh.fit.fe</groupId>
                <artifactId>common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Cấu hình Testcontainers + fixture dùng chung cho test của các service -->
            <dependency>
                <groupId>iuh.fit.fe</groupId>
                <artifactId>common</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- Dùng cho mọi module -->
    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-mysql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Chỉ các module service khai báo plugin này (common là jar thư viện, không repackage) -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
