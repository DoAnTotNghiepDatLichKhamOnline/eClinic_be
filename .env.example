# Copy file này thành .env (đã được .gitignore) rồi chỉnh theo máy của bạn.
# docker-compose.yml và application.yml đều dùng chung các biến này (có giá trị mặc định).

MYSQL_ROOT_PASSWORD=root
DB_HOST=localhost
DB_PORT=3306
DB_NAME=eclinic
DB_USERNAME=eclinic
DB_PASSWORD=eclinic

REDIS_HOST=localhost
REDIS_PORT=6379

PMA_PORT=8081
